//iniciar el proyecto
Iniciar y emparentar
git remote add origin https://github.com/avaroz/ClasificadorFrutasPerceptron.git
Para clonar
git clone https://github.com/avaroz/ClasificadorFrutasPerceptron.git

//En caso de que la rama sea master
git branch -M main

//Hacer un commit
git status
git add .
git commit -m "envio 001"

//Enviar a git
git push -u origin main

//Para instalar cv2
pip install opencv-python opencv-python-headless

//Para instalar libreria pandas que exporta a excel
pip install pandas
pip install openpyxl